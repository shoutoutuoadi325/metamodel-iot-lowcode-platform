{
  "name": "metamodel-iot-lowcode-platform",
  "version": "1.0.0",
  "private": true,
  "description": "IoT Low-Code Platform with Meta-Modeling and Device Orchestration",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "setup": "pnpm install && docker-compose up -d && pnpm --filter @iot-platform/api prisma:migrate",
    "start:infra": "docker-compose up -d",
    "stop:infra": "docker-compose down"
  },
  "keywords": [
    "iot",
    "low-code",
    "mqtt",
    "meta-modeling",
    "orchestration"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
